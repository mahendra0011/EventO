services:
  - type: web
    name: evento-backend
    region: oregon
    plan: free
    env: node
    buildCommand: cd backend ; npm install
    startCommand: cd backend ; npm start
    envVars:
      - key: NODE_ENV
        value: production
